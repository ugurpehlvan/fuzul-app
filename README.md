To run application 
  - To start server
      In main directory
        npm install
        npm start
  - To start frontend
      Navigate to the client directory  => cd client
        npm install
        npm start
